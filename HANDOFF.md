# LUXOR Academy — HANDOFF

**Resume-anywhere document.** If you are a fresh Claude Code session (or human engineer) picking this up: read this top-to-bottom, run the resume command, and you will have full context.

---

## Status (live)

| Field | Value |
|-------|-------|
| **Last updated** | 2026-04-19 |
| **Current session** | v2 — autonomous local build, tier curriculum pivot |
| **Mode** | Autonomous execution, regular commits + push to `main` |
| **Current milestone** | Phase 1 forge-barque Quest validated: MERCURIO 8.93/10 + MARS 91%, both CONDITIONAL-GO, no CRITICAL findings. MERCURIO fixes applied. |
| **Next milestone** | Close the shell integration gap — add `/novice/[track]/[quest]` dynamic route + module HTML static-serve pipeline (MARS biggest flag, non-blocking for content, blocking for end-user rendering). |
| **Blockers** | None |
| **Human gate pending** | None |

## Resume command

```bash
cd /Users/manu/Documents/LUXOR/PROJECTS/luxor-academy
git pull --rebase origin main
cat HANDOFF.md docs/EXECUTION-LOG.md     # this file + the build log
cat .planning/launch-plan-v1.1.md        # the canonical plan (still authoritative)
cat docs/ARCHITECTURE-v2.md              # the shadcn/ui pivot decision record
# then: TaskList to see remaining work, claim the next pending task
```

---

## 1 · Mission (this session)

Build a **three-tier curriculum** (Novice → Experienced → Expert) that teaches LUXOR's methodology by turning Manu's second-brain (169 skills + 55 agents + 103 commands + 10+ shippable repos) into interactive, beautiful, production-grade learning pages.

**Quality bar:** highest possible. Every claim cited. Every module gated by MERCURIO + MARS. Simplicity over sophistication. Ship.

## 2 · Tier map

| Tier | Anchor projects | Core skills | What learners can do afterward |
|------|----------------|-------------|-------------------------------|
| **Novice** | BARQUE, DESIGN-TOOL-PLANNER, NanoBanana | superpowers, brainstorming, codebase-to-course, claude-api | Build & ship real projects using AI as a coding partner |
| **Experienced** | MERCURIO, MARS, CRDT, LibreUIUX | hekat, comonad, ois-orchestrator, categorical-meta-prompting-ts | Orchestrate multi-agent systems with verification gates |
| **Expert** | categorical-meta-prompting, fstar-framework | recursive-meta-prompting, polynomial-functors, fstar-verification, discopy-nlp | Design categorical frameworks and formally verified prompt systems |

Each tier yields **multiple Tracks**; each Track has **multiple Quests**; each Quest has **4–6 Modules**. Canonical last module of every Quest = `ship_your_own`.

## 3 · Architecture (v2 — see `docs/ARCHITECTURE-v2.md` for full decision record)

Two layers, one design system:

- **Shell** (`web/`): Next.js 15 App Router + shadcn/ui + Tailwind v4, **static export** (honors prior "no runtime SSR" invariant). Owns: landing, tier pages, progress dashboard, atlas hub, XP sovereignty, navigation.
- **Modules** (`modules/<track>/<quest>/*.html`): self-contained HTML generated by the `codebase-to-course` skill. Offline-capable, no framework, one file each.
- **Shared design tokens**: `atlas/tokens/tokens.css` imported by shell via Tailwind v4 `@theme` block. Same palette, typography, radii in both layers.

**Invariants preserved from launch-plan-v1.1 (MERCURIO+MARS validated):**

1. Tokens-only theming (no runtime theme JS)
2. Three fonts max (Playfair Display + Inter + JetBrains Mono)
3. SVG/CSS only in modules (no WebGL)
4. 6-slot module contract enforced by `validators/six-slots.js`
5. Every widget declares `sandbox_policy` (CSP + iframe sandbox + no-eval)
6. XP export mandatory (sovereignty)
7. `ship_your_own: true` on every Quest's last module
8. AI gates advise, humans decide
9. Every technical claim cited to a file or official doc

## 4 · Execution protocol

**Per-milestone loop:**

```
plan → build → validate (six-slots + axe + lighthouse) → MERCURIO+MARS parallel verify → commit → push → update HANDOFF + EXECUTION-LOG → next
```

**Commit discipline:**
- One milestone = one atomic commit with clear subject line
- Push to `origin/main` after every commit (remote visibility for Manu + other agents)
- Every commit message ends with `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>`

**Context management:**
- Delegate heavy reading (source-repo spelunking, MERCURIO validation rounds) to subagents via `Agent` tool
- At 70% context window usage: halt current work, snapshot full state to HANDOFF.md + EXECUTION-LOG.md, commit, push, exit. Fresh session resumes.

## 5 · Agentic team

Launched on-demand via `Agent` tool, not all at once:

| Role | Agent / skill | Purpose |
|------|---------------|---------|
| Main orchestrator | this session | Planning, coordination, HANDOFF/EXECUTION-LOG writing |
| Content generator | `codebase-to-course` skill (invoked in subagents) | Module HTML generation |
| Three-plane verifier | `mercurio-orchestrator` agent | Mental/Physical/Spiritual coherence ≥ 8/10 |
| Systems verifier | `MARS` agent | Architectural correctness ≥ 85% |
| Project researcher | `deep-researcher` / `code-explorer` agent | Deep-dive individual LUXOR projects for content extraction |
| Frontend component work | `frontend-architect` agent | shadcn/ui composition, Next.js patterns |

## 6 · Source-of-truth files

| Path | Purpose | Authority |
|------|---------|-----------|
| `HANDOFF.md` | This file — resume anywhere | Lives in main, always current |
| `docs/EXECUTION-LOG.md` | Chronological build log + dashboard | Append-only, timestamped |
| `docs/ARCHITECTURE-v2.md` | Shadcn/ui pivot decision record | Supersedes nothing; extends v1.1 |
| `.planning/launch-plan-v1.1.md` | Canonical pedagogical plan | **Still authoritative** for invariants |
| `.planning/archive/HANDOFF-v0.5.md` | Prior Phase 0.5 handoff | Historical reference |
| `autonomy/META-PROMPT.md` | Academy Forge reentrant prompt | Loop self-coordination (merged from landing/coming-soon) |
| `autonomy/work-queue/priorities.yaml` | Atomic unit queue | Top-down consumption |
| `validators/slots.schema.json` | 6-slot contract | Single source of truth for module shape |
| `atlas/tokens/tokens.css` | Design tokens | Shared by shell + modules |

## 7 · Phase 0.5 substrate state

11 of 12 deliverables shipped on `main` (commits `355c873`, `40ab09c`, `8e8b9e5`, `50a923c`). This session closes item #12 (CI) in the foundation commit.

| Item | Path | Status |
|------|------|--------|
| Validator + fixtures | `validators/six-slots.js` + 6 golden fixtures | ✅ |
| Synergetics SVGs | `atlas/icons/synergetics/*.svg` (6 icons) | ✅ |
| Atlas shell | `atlas/index.html` + web-components + keyboard + XP localStorage | ✅ |
| Pipeline skeleton | `pipelines/codebase-to-course/extract.ts` | ✅ (skeleton — full pipeline is Phase 3) |
| Test infrastructure | Playwright + Lighthouse + axe | ✅ |
| CI workflow | `.github/workflows/ci.yml` | ✅ this commit |

## 8 · Current work queue (live — see `TaskList`)

Completed this session:
1. ✅ Close Phase 0.5: CI workflow
2. ✅ Rewrite HANDOFF.md
3. ✅ Create docs/EXECUTION-LOG.md dashboard
4. ✅ Create docs/ARCHITECTURE-v2.md decision record
5. ✅ Commit + push foundation wave → `d0f27db`
6. ✅ Merge landing/coming-soon assets → `2240dd8`
7. ✅ Scaffold web/ with Next.js 16 + shadcn/ui + 4 building routes
8. ✅ Build three tier landing pages (landing + /novice + /experienced + /expert)

Completed:
9. ✅ BARQUE forge-barque modules generated by content agent → `ede5efd`. All 4 modules pass `node validators/six-slots.js`. Fixtures still 6/6. HTML 23-27 KB each (budget was 80 KB). Module 03 content rewritten when agent discovered priorities.yaml hint mismatched real BARQUE code — anti-confabulation discipline held.

Completed:
10. ✅ MERCURIO three-plane verification → 8.93/10 composite (9.1 Mental / 8.4 Physical / 9.3 Spiritual), CONDITIONAL-GO. Report: `.planning/validation-round-phase-1-mercurio.md`.
10a. ✅ MARS systems verification → 91% confidence, 5 checks (PARTIAL PASS on integration coherence due to missing shell route; everything else PASS), CONDITIONAL-GO. Report: `.planning/validation-round-phase-1-mars.md`.
10b. ✅ MERCURIO MEDIUM fixes applied: Module 03 line-ref corrected (subprocess.run at 115, env= at 121 — both cited); Module 04 XP split 500 → 150 now + 350 deferred (with primer explanation); quest total updated 675 → 325 with deferred note. All 4 modules re-validate PASS. Fixtures still 6/6.
10c. ✅ Consolidated validation report written at `.planning/validation-round-phase-1.md`.

Next session — three ordered items:

11. **MARS integration gap close** (blocks end-user rendering of the Quest):
    - Create `web/app/novice/[track]/[quest]/page.tsx` — dynamic route reading the `*.module.json` + rendering a module-list overview with links out to each module's `.html`
    - Add npm script `prebuild:modules` that syncs `modules/**/*.html` into `web/public/modules/` before `next build`
    - Verify `/novice/build-and-ship/forge-barque` renders 4 clickable module cards
    - Re-run `npm run build` — expect 5 static routes + 4 module HTMLs served
12. **Refresh-sources pipeline** (unblocks publication, closes MERCURIO LOW):
    - Write `scripts/refresh-sources.sh <repo-name> <upstream-url>` that clones, captures HEAD SHA, archives to `content/sources/<name>/`, and updates `content/sources/VENDOR.json`
    - Run it for barque; re-generate all 4 module permalinks to use the pinned SHA
    - Re-validate
13. **Human gate `phase-1`** (only after #11 + #12):
    - Write `.planning/human-gate-phase-1.md` with the 4 permalinks, browser screenshots, and the MERCURIO+MARS verdicts consolidated
    - Manu reviews each module HTML in a browser; confirms keyboard traversal + XP round-trip + Module 04 form stub
    - Manu sets `autonomy/loop-state.json.human_approval["phase-1"] = true`
14. **Experienced + Expert tier first quests** (after `phase-1` gate passes):
    - Experienced: `orchestration/mercurio-for-decisions` (5 modules)
    - Expert: `categorical-meta-prompting/cmp-foundations` (6 modules)
    - Both via the same codebase-to-course agent pattern that shipped forge-barque

## 9 · If you are resuming

1. `git pull --rebase origin main`
2. Read this file + `docs/EXECUTION-LOG.md` (find the last entry — that's where work stopped)
3. Run `TaskList` — the first pending task with no owner is your next work
4. Read `autonomy/META-PROMPT.md` if you are running as the Academy Forge (autonomous loop)
5. Before writing code: verify substrate green → `npm ci && npm run validate:fixtures` should return `6 passed, 0 failed`
6. Before claiming work "complete": run MERCURIO + MARS subagents in parallel against the artifact

## 10 · If you must halt (70% context reached or unforeseen blocker)

1. Write final state to EXECUTION-LOG.md (one line per operation since last entry)
2. Update this file's **Status** table + **Current work queue**
3. Commit + push (even partial — better to capture state than preserve "clean" commits)
4. Exit cleanly — do not start new work

**Simplicity is the ultimate sophistication.** One HANDOFF, one EXECUTION-LOG, one main branch, one tier structure. Resist the temptation to build machinery before content.
